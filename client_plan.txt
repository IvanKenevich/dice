                       CLIENT
Talks to the serverold, throws dice, interacts with the user

class Client {
    main() {
        // Make a socket
        // Connect it to serverold
	// enter a loop(sequence?) that reads commands from the serverold
    }
}
