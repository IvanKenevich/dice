                       CLIENT
Talks to the server, throws dice, interacts with the user

class Client {
    main() {
        // Make a socket
        // Connect it to server
	// enter a loop(sequence?) that reads commands from the server
    }
}
